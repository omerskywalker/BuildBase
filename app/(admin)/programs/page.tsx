// TODO Batch 7: Admin program + workout template editor
// - Phase editor (name, week ranges)
// - Session editor with dnd-kit drag-and-drop
// - Exercise library CRUD
// - Weight editor for all tier/gender combinations
export default function AdminProgramsPage() {
  return (
    <div>
      <h1 style={{ fontSize: 24, fontWeight: 700, color: "#E8F0E8", fontFamily: "var(--font-space-grotesk)", marginBottom: 4 }}>
        Program Editor
      </h1>
      <p style={{ color: "#8A9E8A", fontSize: 14 }}>Workout template editor — coming in Batch 7.</p>
    </div>
  );
}
